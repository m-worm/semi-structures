# pyproject.toml is the single source of truth for dependencies. This file
# exists only for tools that expect a requirements file, and it installs the
# package with every extra, which is what the examples need.
#
#   core only (process DSL, vector and section renderers):
#       python -m pip install -e .
#   everything (adds the solid backend, the MCP server and the test tools):
#       python -m pip install -e ".[all]"
#
-e .[all]
